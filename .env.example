# LLM(OpenAI 兼容;换供应商只改这三个变量,代码零改动)
DEEPSEEK_API_KEY=sk-xxx
# DEEPSEEK_BASE_URL=https://api.deepseek.com  # 默认;兜底可换 SiliconFlow / 火山方舟
# DEEPSEEK_MODEL=deepseek-chat

# 限流(Vercel Marketplace 集成 Upstash Redis 后自动注入;缺省时限流关闭 = fail-open)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# GLOBAL_DAILY_LIMIT=300

# Origin 白名单(逗号分隔;缺省不校验)
# ALLOWED_ORIGINS=https://cv.remi.run
